// Using jQuery to listen for a click event on the DIV#update_header
$('#update_header').click(function() {
    // Updating the text of the <header> element
    $('header').text('New Header!!!');
});
