// I am using jQuery to listen for a click event on the DIV#red_header
$('#red_header').click(function() {
    // When clicked, add the class 'red' to the <header>
    $('header').addClass('red');
});
